
services:
  frontend:
    build:
      context: ./client
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build:
      context: ./server
    ports:
      - "3001:3001"
    volumes:
      - ./uploads:/app/uploads

